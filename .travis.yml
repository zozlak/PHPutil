language: php
php:
  - 5.6
  - 7.0
  - 7.1
  - 7.2

services:
  - postgresql
  
install:
  - composer require php-coveralls/php-coveralls

before_script:
  - mkdir -p build/logs

after_success:
  - php vendor/bin/php-coveralls -v

